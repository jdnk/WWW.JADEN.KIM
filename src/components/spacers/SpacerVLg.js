import * as React from 'react'
import { Spacing } from '../../styles'

const SpacerVLg = () => (
  <div style={styles.container} />
)

const styles = {
  container: {
    ...Spacing.spacing.verLg,
  },
}

export default SpacerVLg
